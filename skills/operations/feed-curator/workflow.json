{
  "_comment": "Declarative workflow definition for feed-curator. Keeps the education-feeds catalog accurate by validating existing feeds, discovering candidates, proposing a human-reviewable change set, and auto-applying only mechanically-safe repairs — all logged for audit/revert.",
  "id": "feed-curator",
  "description": "Catalog librarian: validate feeds → discover candidates → propose changes → auto-apply safe repairs → audit log → quality gate",
  "steps": [
    {"step": 1, "action": "validate",   "input_from": "shared/feeds/feeds.json",         "output_to": "step2",
     "uses": "tools/seed_curator.py --validate",
     "note": "Run F2 currency engine over every feed: dead (404/410), unreachable, stale, redirect detected, label sanity"},
    {"step": 2, "action": "discover",   "input_from": "authoritative page URL (optional)", "output_to": "step3",
     "uses": "tools/seed_curator.py --discover-from PAGE_URL",
     "note": "RSS autodiscovery (rel=alternate); polite, robots.txt-respecting; candidates are proposals, never auto-added"},
    {"step": 3, "action": "propose",    "input_from": "step1.health + step2.candidates",  "output_to": "step4",
     "uses": "tools/seed_curator.py --propose",
     "note": "Emit one human-reviewable change set: add/remove/repair/relabel, each tagged safe_repair, with reason + source + confidence"},
    {"step": 4, "action": "apply",      "input_from": "step3.proposal",                  "output_to": "step5",
     "uses": "tools/seed_curator.py --apply",
     "note": "AUTO-APPLY only mechanically-safe repairs (confirmed 404/410 remove, verified 301/302 follow). Everything else needs human --apply-proposal."},
    {"step": 5, "atom": "quality-check", "input_from": "catalog_health_report",     "output_to": "final",
     "gates": ["no_fabricated_urls", "audit_log_written", "safe_repairs_only_auto_applied"]}
  ],
  "shortcut_atoms": [],
  "note": "Every change — auto or approved — is appended to ledger/feeds-change-log.json and is reversible with --revert. Only tier:canonical / authority:primary confirms a change. Never fabricate a feed URL or verified status."
}
