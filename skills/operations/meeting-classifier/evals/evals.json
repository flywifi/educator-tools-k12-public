{
  "skill_name": "meeting-classifier",
  "_note": "Each case feeds scripts/classify_meeting.py (offline). assertions describe the expected classification record fields. Real student data is never used — student_ref points at placeholder profiles in shared/students/students.example.json.",
  "evals": [
    {
      "kind": "prompt",
      "name": "iep_annual_required_and_escalates",
      "input": {
        "title": "IEP Annual Review",
        "from_title": "ESE Case Manager",
        "attendees": [
          "LEA rep",
          "case manager"
        ],
        "user_request": "help me prepare",
        "student_ref": "S-000456"
      },
      "assert": {
        "meeting_type": "iep_meeting",
        "recommended_next_skill": "special-education-support",
        "escalate_to_human": true,
        "required_cadence.required": true,
        "required_cadence.authority": "IDEA",
        "required_cadence.verify_on_source": true
      },
      "_note": "Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run."
    },
    {
      "kind": "prompt",
      "name": "section_504_required",
      "input": {
        "title": "504 plan meeting",
        "from_title": "504 Coordinator",
        "user_request": "agenda for this"
      },
      "assert": {
        "meeting_type": "section_504_meeting",
        "recommended_next_skill": "special-education-support",
        "required_cadence.authority": "Section 504",
        "escalate_to_human": true
      },
      "_note": "Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run."
    },
    {
      "kind": "prompt",
      "name": "observation_vs_interim_minority_report",
      "input": {
        "title": "Observation on Tuesday",
        "body": "I'll stop by your room",
        "user_request": "what to expect"
      },
      "assert": {
        "meeting_type": "annual_review_observation",
        "confidence": "low",
        "minority_report.alternates": [
          "interim_observation"
        ]
      },
      "_note": "Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run."
    },
    {
      "kind": "prompt",
      "name": "formal_observation_corroborated_no_minority",
      "input": {
        "title": "Formal observation / evaluation",
        "from_title": "Principal",
        "body": "summative evaluation using the Marzano rubric",
        "user_request": "help me prepare"
      },
      "assert": {
        "meeting_type": "annual_review_observation",
        "confidence": "high",
        "minority_report": null,
        "recommended_next_skill": "professional-learning"
      },
      "_note": "Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run."
    },
    {
      "kind": "prompt",
      "name": "observation_evaluator_persona_routes_to_admin",
      "input": {
        "title": "Formal observation",
        "persona": "administrator",
        "body": "Marzano evaluation"
      },
      "assert": {
        "meeting_type": "annual_review_observation",
        "recommended_next_skill": "school-administration"
      },
      "_note": "Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run."
    },
    {
      "kind": "prompt",
      "name": "parent_conference_routes_family_communication",
      "input": {
        "title": "Parent-teacher conference",
        "from_title": "parent",
        "user_request": "talking points"
      },
      "assert": {
        "meeting_type": "parent_teacher_conference",
        "recommended_next_skill": "family-communication"
      },
      "_note": "Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run."
    },
    {
      "kind": "prompt",
      "name": "medical_parent_contact_surfaces_action_plan_and_escalates",
      "input": {
        "user_request": "I need to call home, the student had an allergic reaction",
        "from_title": "School Nurse",
        "student_ref": "S-000123"
      },
      "assert": {
        "escalate_to_human": true,
        "medical_action_plan.safety_critical": true,
        "recommended_next_skill": "family-communication"
      },
      "_note": "Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run."
    },
    {
      "kind": "prompt",
      "name": "mtss_routes_intervention",
      "input": {
        "title": "MTSS problem-solving / Tier 2",
        "user_request": "prep an intervention plan"
      },
      "assert": {
        "meeting_type": "mtss_meeting",
        "recommended_next_skill": "intervention-mtss"
      },
      "_note": "Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run."
    },
    {
      "kind": "prompt",
      "name": "department_meeting_routes_professional_learning",
      "input": {
        "title": "Math department meeting",
        "user_request": "agenda"
      },
      "assert": {
        "meeting_type": "department_meeting",
        "recommended_next_skill": "professional-learning"
      },
      "_note": "Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run."
    },
    {
      "kind": "prompt",
      "name": "schedule_triage_intent",
      "input": {
        "user_request": "what are today's meetings and how should I prioritize"
      },
      "assert": {
        "request_intent": "schedule_or_triage"
      },
      "_note": "Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run."
    },
    {
      "kind": "prompt",
      "name": "safety_training_manual_review",
      "input": {
        "title": "Active-threat safety training",
        "user_request": "what is this"
      },
      "assert": {
        "meeting_type": "safety_training",
        "recommended_next_skill": "manual_review"
      },
      "_note": "Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run."
    },
    {
      "kind": "prompt",
      "name": "weak_evidence_unknown",
      "input": {
        "title": "quick meeting",
        "user_request": "help with this meeting"
      },
      "assert": {
        "meeting_type": "unknown",
        "recommended_next_skill": "manual_review"
      },
      "_note": "Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run."
    },
    {
      "kind": "prompt",
      "name": "connector_degradation_lowers_confidence",
      "input": {
        "title": "Observation",
        "body": "stop by"
      },
      "flags": {
        "connectors": {
          "google_workspace": "permission_blocked"
        }
      },
      "assert": {
        "degraded": true
      },
      "_note": "Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run."
    },
    {
      "kind": "prompt",
      "name": "id_only_mode_uses_student_id_not_name",
      "input": {
        "title": "IEP meeting",
        "from_title": "case manager",
        "student_ref": "S-000123"
      },
      "flags": {
        "student_identification": {
          "mode": "id"
        }
      },
      "assert": {
        "subject_student": "student S-000123"
      },
      "_note": "Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run."
    },
    {
      "kind": "prompt",
      "name": "ics_upload_classifies_parent_conference",
      "_note": "A dropped .ics invite is read via shared/docintel (no retyping) and folded into the clues. Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run.",
      "input": {
        "files": [
          "skills/meeting-classifier/examples/sample-invite.ics"
        ],
        "user_request": "talking points"
      },
      "assert": {
        "meeting_type": "parent_teacher_conference",
        "recommended_next_skill": "family-communication"
      }
    },
    {
      "kind": "prompt",
      "name": "eml_upload_classifies_observation",
      "_note": "A saved .eml from the principal is read via shared/docintel and drives the classification. Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run.",
      "input": {
        "files": [
          "skills/meeting-classifier/examples/sample-observation.eml"
        ],
        "user_request": "help me prepare"
      },
      "assert": {
        "meeting_type": "annual_review_observation",
        "recommended_next_skill": "professional-learning"
      }
    },
    {
      "kind": "prompt",
      "name": "vtt_transcript_classifies_iep",
      "_note": "A dropped .vtt video-call transcript is read via shared/docintel (captions -> text) and classified. Reclassified 2026-08-17: this has `input`+`assert` but NO code classifies meeting evidence — shared/routing/router.py:meeting_route() maps an ALREADY-KNOWN meeting_type to a skill; inferring the type from a title/sender/attendees is the skill's job, done by a model. Calling it a `call` case would assert a callable that does not exist. The structured `assert` block is kept as the expected output shape for a recorded run.",
      "input": {
        "files": [
          "skills/meeting-classifier/examples/sample-meeting.vtt"
        ],
        "user_request": "summarize this meeting"
      },
      "assert": {
        "meeting_type": "iep_meeting",
        "recommended_next_skill": "special-education-support",
        "escalate_to_human": true
      }
    }
  ]
}
