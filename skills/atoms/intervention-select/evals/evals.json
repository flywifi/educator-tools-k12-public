{
  "skill_name": "intervention-select",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_suggest_intervention",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "suggest intervention",
      "assert": {
        "atom": "intervention-select",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_tier_2_intervention",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "tier 2 intervention",
      "assert": {
        "atom": "intervention-select",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_evidence-based_intervention",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "evidence-based intervention",
      "assert": {
        "atom": "intervention-select",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
