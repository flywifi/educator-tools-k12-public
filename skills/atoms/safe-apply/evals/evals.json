{
  "skill_name": "safe-apply",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_classify_changes",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "classify changes",
      "assert": {
        "atom": "safe-apply",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_which_fixes_are_safe",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "which fixes are safe",
      "assert": {
        "atom": "safe-apply",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_mechanical_vs_judgment",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "mechanical vs judgment",
      "assert": {
        "atom": "safe-apply",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
