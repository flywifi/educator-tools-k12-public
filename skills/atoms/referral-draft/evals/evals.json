{
  "skill_name": "referral-draft",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_draft_a_referral",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "draft a referral",
      "assert": {
        "atom": "referral-draft",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_mtss_referral",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "mtss referral",
      "assert": {
        "atom": "referral-draft",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_evaluation_referral",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "evaluation referral",
      "assert": {
        "atom": "referral-draft",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
