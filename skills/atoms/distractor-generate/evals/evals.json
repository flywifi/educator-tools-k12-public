{
  "skill_name": "distractor-generate",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_distractors",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "distractors",
      "assert": {
        "atom": "distractor-generate",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_wrong_answers",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "wrong answers",
      "assert": {
        "atom": "distractor-generate",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_plausible_choices",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "plausible choices",
      "assert": {
        "atom": "distractor-generate",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
