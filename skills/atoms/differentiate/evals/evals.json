{
  "skill_name": "differentiate",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_make_this_ell-friendly",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "make this ell-friendly",
      "assert": {
        "atom": "differentiate",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_add_iep_supports",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "add iep supports",
      "assert": {
        "atom": "differentiate",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_differentiate_this",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "differentiate this",
      "assert": {
        "atom": "differentiate",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
