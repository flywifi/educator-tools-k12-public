{
  "skill_name": "standards-crosswalk",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_crosswalk_standards",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "crosswalk standards",
      "assert": {
        "atom": "standards-crosswalk",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_map_to_another_framework",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "map to another framework",
      "assert": {
        "atom": "standards-crosswalk",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_equivalent_standard",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "equivalent standard",
      "assert": {
        "atom": "standards-crosswalk",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
