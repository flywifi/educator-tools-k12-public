{
  "skill_name": "iep-goal",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_draft_an_iep_goal",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "draft an iep goal",
      "assert": {
        "atom": "iep-goal",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_one_iep_goal",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "one iep goal",
      "assert": {
        "atom": "iep-goal",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_write_a_goal_for",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "write a goal for",
      "assert": {
        "atom": "iep-goal",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
