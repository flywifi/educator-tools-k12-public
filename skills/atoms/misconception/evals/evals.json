{
  "skill_name": "misconception",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_misconception",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "misconception",
      "assert": {
        "atom": "misconception",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_what_do_students_get_wrong",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "what do students get wrong",
      "assert": {
        "atom": "misconception",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_common_mistakes",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "common mistakes",
      "assert": {
        "atom": "misconception",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
