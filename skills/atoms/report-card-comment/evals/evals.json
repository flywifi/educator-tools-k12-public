{
  "skill_name": "report-card-comment",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_report_card_comment",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "report card comment",
      "assert": {
        "atom": "report-card-comment",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_narrative_comment",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "narrative comment",
      "assert": {
        "atom": "report-card-comment",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_standards-based_comment",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "standards-based comment",
      "assert": {
        "atom": "report-card-comment",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
