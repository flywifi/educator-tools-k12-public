{
  "skill_name": "govern-artifact",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_stamp_provenance",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "stamp provenance",
      "assert": {
        "atom": "govern-artifact",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_attach_governance",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "attach governance",
      "assert": {
        "atom": "govern-artifact",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_artifact_lineage",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "artifact lineage",
      "assert": {
        "atom": "govern-artifact",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
