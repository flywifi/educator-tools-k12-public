{
  "skill_name": "wizard-interview",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_run_the_intake_wizard",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "run the intake wizard",
      "assert": {
        "atom": "wizard-interview",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_guided_intake",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "guided intake",
      "assert": {
        "atom": "wizard-interview",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_interview_questions",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "interview questions",
      "assert": {
        "atom": "wizard-interview",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
