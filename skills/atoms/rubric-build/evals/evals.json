{
  "skill_name": "rubric-build",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_build_a_rubric",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "build a rubric",
      "assert": {
        "atom": "rubric-build",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_make_a_rubric",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "make a rubric",
      "assert": {
        "atom": "rubric-build",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_scoring_rubric",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "scoring rubric",
      "assert": {
        "atom": "rubric-build",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
