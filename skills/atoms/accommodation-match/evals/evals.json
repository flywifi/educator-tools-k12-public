{
  "skill_name": "accommodation-match",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_match_accommodations",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "match accommodations",
      "assert": {
        "atom": "accommodation-match",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_suggest_accommodations",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "suggest accommodations",
      "assert": {
        "atom": "accommodation-match",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_accommodation_for",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "accommodation for",
      "assert": {
        "atom": "accommodation-match",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
