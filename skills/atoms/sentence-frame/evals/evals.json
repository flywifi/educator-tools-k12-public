{
  "skill_name": "sentence-frame",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_sentence_frame",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "sentence frame",
      "assert": {
        "atom": "sentence-frame",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_sentence_stem",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "sentence stem",
      "assert": {
        "atom": "sentence-frame",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_language_scaffold",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "language scaffold",
      "assert": {
        "atom": "sentence-frame",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
