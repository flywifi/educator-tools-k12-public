{
  "skill_name": "meeting-agenda",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_create_an_agenda",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "create an agenda",
      "assert": {
        "atom": "meeting-agenda",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_meeting_agenda",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "meeting agenda",
      "assert": {
        "atom": "meeting-agenda",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_agenda_for",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "agenda for",
      "assert": {
        "atom": "meeting-agenda",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
