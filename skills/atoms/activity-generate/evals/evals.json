{
  "skill_name": "activity-generate",
  "evals": [
    {
      "kind": "call",
      "name": "routes_from_one_activity",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "one activity",
      "assert": {
        "atom": "activity-generate",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_give_me_an_activity",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "give me an activity",
      "assert": {
        "atom": "activity-generate",
        "basis": "atom_keyword_match"
      }
    },
    {
      "kind": "call",
      "name": "routes_from_single_activity",
      "_note": "Deterministic: shared/routing/router.py is pure and offline, so this needs no model. The phrase is taken from this atom's own routing.json entry, so the case cannot drift from the registry it tests.",
      "call": "routing.router:atom_route",
      "input": "single activity",
      "assert": {
        "atom": "activity-generate",
        "basis": "atom_keyword_match"
      }
    }
  ]
}
