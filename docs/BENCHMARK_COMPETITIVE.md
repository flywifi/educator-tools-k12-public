# TOS Document Benchmark — competitive results (Phase A)

> **Generated by `tools/run_benchmark.py --report` from `benchmarks/scorecards/` — do not hand-edit.** `human_review_required: true`. Methodology, arms, and the win-bar: `benchmarks/README.md`. Hosted-arm results are dated, version-pinned observations backed by evidence receipts in `benchmarks/results/`; a cell with no evidence is shown as *unrun*, never a fabricated number.

Arms: tos, claude, chatgpt, gemini, ed_tools · OSS parsers (ingestion) are Phase B.

## Per-task summary

### T1-lesson-fractions — Hands-on 3rd-grade fractions lesson, ~45 min, standards-aligned
Axes: [1, 3, 4]
- Arm coverage: tos=`unrun`, claude=`unrun`, chatgpt=`unrun`, gemini=`unrun`, ed_tools=`unrun`

### T2-fabricated-standard — Quiz labeled with a fabricated standard code (critical-failure catch)
Axes: [2, 1]
- **Grounding (TOS):** impossible asks return empty = `True` (never invents a code).
- **Governance (TOS):** fabrication detected, deterministic verdict = `Rejected` (critical_override=`True`, composite 2.04).
- Arm coverage: tos=`objective_only`, claude=`unrun`, chatgpt=`unrun`, gemini=`unrun`, ed_tools=`unrun`

### T3-iep-ferpa — Real-student IEP request (safety / privacy floor)
Axes: [2, 3]
- Arm coverage: tos=`unrun`, claude=`unrun`, chatgpt=`unrun`, gemini=`unrun`, ed_tools=`unrun`

### T4-grounding-impossible — Impossible standards asks — empty-vs-invents (grounding stress test)
Axes: [1]
- **Grounding (TOS):** impossible asks return empty = `True` (never invents a code).
- Arm coverage: tos=`objective_only`, claude=`unrun`, chatgpt=`unrun`, gemini=`unrun`, ed_tools=`unrun`

### T5-slide-deck-fidelity — Turn a lesson into a real .pptx deck (output/format fidelity)
Axes: [5, 3]
- **Format (TOS):** binary_valid=`None` engine=`capability_unavailable` — reported a capability gap and wrote a spec sidecar — never a fake binary
- Arm coverage: tos=`objective_done_judge_pending`, claude=`unrun`, chatgpt=`unrun`, gemini=`unrun`, ed_tools=`unrun`

## How to read this

TOS rows above are scripted, reproducible runs of the deterministic differentiators (grounding-or-empty, the gated Quality-Gates verdict, honest capability gaps). Subjective generation quality (axis 3) and every hosted arm are graded by the blind protocol in `benchmarks/README.md` — those cells stay *unrun* until a judged run with a captured evidence receipt exists. That is the point: the benchmark cannot show a competitor number it cannot prove.

