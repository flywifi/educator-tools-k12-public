# Optional dependencies for tools/standards_refresh.py (the standards-updater).
# The crawler runs on the Python standard library alone; these only make fetching
# and parsing nicer, and enable JS-rendered pages (e.g., the CPALMS standards search).
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
# JS-rendered SPA pages only (e.g., CPALMS search) — install one if needed:
# selenium>=4.15.0
# playwright>=1.40.0
